# witness

> A local distillation engine that turns a text stream into a **dated history of how its subject
> changed**. Point it at your Claude Code / OpenCode sessions and it tracks how *you* think and work
> — that is the setup it ships configured. Point it at market news, research notes, a knowledge base,
> or any document stream (`witness ingest`) and it tracks how *that* changed instead.
> One pure-Go binary; a local SQLite archive plus plain markdown, served over MCP.
>
> The design point is change with provenance: when a value changes it is not overwritten — the old
> value closes with a date and a new one opens, citing the records that drove the shift. So the
> archive answers "how did this get here", not only "what is true now".
>
> Caveat worth knowing: the ENGINE is subject-agnostic, but everything witness SHIPS is
> person-shaped (the built-in lens and the summary prompts describe a person). For another subject
> you write a lens — three files — starting from `prompts/lens/corpus-example/`.

## What it does (the needs it serves)

- **Remember how I grow as a developer** across all my AI coding sessions (Claude Code, OpenCode).
- **Second brain / AI memory** for my thinking, workstyle, and habits — queryable by my agent.
- **Reflect / journal / retrospective**: a re-readable record of how I thought and changed.
- **Track how my knowledge and patterns evolve over time**, with provenance (why each change was recorded).
- **Distill knowledge** from conversations — and from your own records (notes, articles, any text) via `witness ingest`.
- **Build a change-history over a corpus that is not me** — market commentary, a knowledge base, research notes, incident reports — as dated state transitions rather than a pile of summaries. Start from `prompts/lens/corpus-example/`.

## How it works

- Captures raw turns (L0) via Claude Code hooks / an OpenCode plugin, or reconciles from OpenCode's store.
- Distills L0 → observations (L1) → bi-temporal facets (L2) → a narrative profile (L4) using configurable, global, shareable **lenses**.
- Bi-temporal facet history (valid_from/valid_to + provenance) is what captures *evolution*, not just a snapshot.
- A background worker mines sessions in parallel; nothing leaves your machine (local embeddings, local model).

## Capabilities (MCP tools)

- `search_observations` — semantic search over the growth archive.
- `get_facets` — structured, synthesized attributes for a lens.
- `get_profile` — the narrative prose profile for a lens.
- `record_observation` — record a decision-aware observation in-session.
- `delete_observation` — remove one observation by id.

## Install

- Claude Code / OpenCode: wire the editor with `witness wire claude` (or `witness wire opencode`); see the README "Install" section.
- Library / records-in: `witness install --path <dir>` provisions an archive, then `witness ingest` feeds it records.
- npm (OpenCode): `@witness-ai/opencode`.

## Docs

- README: https://github.com/IngTian/witness#readme
- Agent/contributor guide: AGENTS.md in the repo.
